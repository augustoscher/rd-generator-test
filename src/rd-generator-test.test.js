describe('utils tests', () => {
  it('baita teste', () => {
    const date = new Date();
    expect(date).toBeDefined();
  });

  it('baita teste2', () => {
    const date = new Date();
    expect(date).toBeDefined();
  });
});
